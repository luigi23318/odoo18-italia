<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_move_form_om_sync_button" model="ir.ui.view">
        <field name="name">account.move.form.om.sync.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header" position="inside">
                <button
                    name="action_om_sync_edi_details"
                    string="Ricalcola dettagli e-fattura"
                    type="object"
                    class="btn-secondary"
                    invisible="move_type not in ('out_invoice', 'out_refund') or state != 'draft' or country_code != 'IT'"
                    help="Forza il ricalcolo manuale dei campi del tab 'Dettagli e-fattura'. In automatico il ricalcolo avviene già al cambio righe e alla conferma della fattura."
                />
            </xpath>

        </field>
    </record>

</odoo>
